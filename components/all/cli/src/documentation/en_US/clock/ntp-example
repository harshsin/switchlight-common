ntp enable
  Enables NTP service.

no ntp enable
  Disables NTP service.

ntp server 0.us.pool.ntp.org; ntp server 1.us.pool.ntp.org
  Configures the device to use the two specified NTP server pools.
