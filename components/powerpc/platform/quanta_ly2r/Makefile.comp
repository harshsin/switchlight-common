# -*- Makefile -*-
################################################################################
#
# Quanta LY2R
#
###############################################################################
ifndef SWITCHLIGHT
$(error $$SWITCHLIGHT is not set)
else
include $(SWITCHLIGHT)/make/config.mk
endif

KERNEL.BIN.GZ := $(shell $(SWITCHLIGHT_PKG_INSTALL) kernel-85xx:powerpc --find-file kernel-85xx.bin.gz)
INITRD := $(shell $(SWITCHLIGHT_PKG_INSTALL) initrd-powerpc:powerpc --find-file initrd-powerpc)
DTB := $(shell $(SWITCHLIGHT_PKG_INSTALL) kernel-85xx-dtbs:powerpc --find-file quanta_ly2r.dtb)
PLATFORM_NAME := quanta-ly2r

# no loader size indicate, loader and jffs2 are not contiguous
FLASHFS_SIZE_KB := 32768
FLASH_ERASEBLOCK_KB := 128
SWI_URL := flash2:.ztn-switchlight.swi

include $(SWITCHLIGHT)/make/loader-ppc.mk

.PHONY: deb

deb:
	$(MAKE) -C deb 
